/** Bump to reset persisted TanStack DB catalog sync state on disk (see TanStack `schemaVersion` docs). */
export const APP_CATALOG_DATA_SCHEMA_VERSION = 1;

/** Bump to reset persisted TanStack DB sales collections on disk. */
export const APP_SALES_DATA_SCHEMA_VERSION = 1;
