name: desktech-pos
processes:
  build:web:
    command: bun run build
    working_dir: apps/app
  expo:dev:
    command: bunx expo start
    working_dir: apps/app
    auto_start: true
  sst:dev:
    command: bun run sst dev
    auto_start: true
